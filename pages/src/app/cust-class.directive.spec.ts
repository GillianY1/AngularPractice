import { CustClassDirective } from './cust-class.directive';

describe('CustClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CustClassDirective();
    expect(directive).toBeTruthy();
  });
});
